<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh">
	<title>门泊吴船亦已谋 - 分布式</title>
	<subtitle>记一下我都整了些什么玩意。</subtitle>
	<link href="https://paakmau.github.io/categories/fen-bu-shi/atom.xml" rel="self" type="application/atom+xml"/>
  <link href="https://paakmau.github.io"/>
	<generator uri="https://www.getzola.org/">Zola</generator>
	<updated>2022-04-26T15:08:36+00:00</updated>
	<id>https://paakmau.github.io/categories/fen-bu-shi/atom.xml</id>
	<entry xml:lang="zh">
		<title>Kademlia 算法详解</title>
		<published>2022-04-26T15:08:36+00:00</published>
		<updated>2022-04-26T15:08:36+00:00</updated>
		<link href="https://paakmau.github.io/202204261508/" type="text/html"/>
		<id>https://paakmau.github.io/202204261508/</id>
		<content type="html">&lt;p&gt;建立 P2P 网络时我们需要面对的一个问题就是一个节点如何高效访问网络中的其他节点中存储的资源。
Kademlia 算法在各个节点上使用某种策略建立路由表以支持节点之间的高效访问，并且支持在网络中进行资源存储与访问。&lt;&#x2F;p&gt;
&lt;span id=&quot;continue-reading&quot;&gt;&lt;&#x2F;span&gt;&lt;h2 id=&quot;lu-you-biao-wei-hu&quot;&gt;路由表维护&lt;&#x2F;h2&gt;
&lt;p&gt;先不考虑资源的部分，我们希望网络中的两个节点能够高效的连接。
每个节点上都需要维护一个路由表，路由表中的每一条记录包括一个其他节点的地址等信息，表示这两个节点之间能够直接相连。
把这个直接相连看作是边，那么边和节点就构成一张图。
两个非直连的节点想要连接就只能经过图上的一条路径，我们希望这条路径的长度能控制在一个合理的范围内。
为什么一定要走一条路径呢？
因为路由表不可能无限大，你不可能把网络中所有节点都塞进去，内存会爆。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;si-lu&quot;&gt;思路&lt;&#x2F;h3&gt;
&lt;p&gt;有没有一种可能，可以用二分来设计路由表的维护策略，使得寻找某个节点时路由转发的次数降到 log 级别。
Kademlia 就是这样干的。&lt;&#x2F;p&gt;
&lt;p&gt;它要求每个节点自己生成一个唯一的无符号整型 ID。
至于怎么唯一，只要随机出来的范围足够大，那么就能认为 ID 冲突的概率可以忽略不计。
所以我们把 ID 的范围整大一点，这里就使用常见的 160 位。
然后把这个 ID 当成二进制来看，也就是看作是零一序列。
这个零一序列是不是可以看成是一棵二叉树上的路径：从高位到低位，零的话往左，一的话往右。
把这条路径看作是根节点到叶子的路径，于是每个 ID 对应一个路径，也对应一个叶子节点。
于是网络中所有节点的 ID 就构成了一棵二叉树。&lt;&#x2F;p&gt;
&lt;p&gt;这个时候，我们希望有一种路由策略，对于一个源节点，给出一个目标节点的 ID，能够在 log 级的时间内，在网络中查询到目标节点。
当然路由表的空间复杂度也要是 log 级。
于是对于源节点来说，我们发现它在二叉树上的路径把整个二叉树切割成了若干个子树。
假设这条路径是从根节点出发，那么路径每走一个节点就会切出来一个子树，已走的路径越长，子树的根在整棵二叉树中的深度也就越大。
显然这些子树的这个深度都是唯一且递增的，为了方便，我们就用这个深度来称呼它们（也可以当作是编号）。
目标节点一定会在这些子树的其中一棵里面。
然后惊奇的发现我们可以通过目标节点的 ID 确定它在哪棵子树中：通过检查目标节点与源节点的 ID 的前缀有多少位相同，就知道它在哪个深度的子树上。
更具体的说就是对目标节点与源节点的 ID 做一个异或，再找到异或结果的最高位，这个最高位是第几位，所在子树的深度就是几。
这个异或的结果也就是 Kademlia 算法中所说的节点之间的“距离”。
找到子树之后就好办了，这个时候对于源节点来说，我们只要能够访问到这个子树中的任意一个节点，再把它作为新的源节点重复执行查询目标节点的操作，然后估算一下复杂度，神奇的事情就发生了。
因为根据刚刚说的操作，这个新的源节点是跟目标节点在同一个子树里的，所以它们的 ID 前缀相同的位比起最初的源节点相同的位，就会至少会多一位。
如果新的源节点重复执行这个操作，下一个源节点前缀相同的位又会至少多一位。
因为这里假设 ID 的位数是 160 位，所以最坏情况下跑 160 次也能找到目标节点，复杂度是 log 级的。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;shi-xian&quot;&gt;实现&lt;&#x2F;h3&gt;
&lt;p&gt;原理弄清楚了，接下来的问题就是具体实现。&lt;&#x2F;p&gt;
&lt;p&gt;其一是路由表的结构。
在每个节点的机器上，ID 对应的路径切出来一堆子树，对于每棵子树，理论上我们存一个节点就够了。
但是 P2P 网络中节点是会失效的，所以我们多存几个，比如说 K 个。
这个 K 是不是很眼熟，如果看过别的资料，你就会发现这个子树实际就是 Kademlia 中的 k-bucket。
所以一种实现就是每棵子树开一个 k-bucket，限制它的最大容量为 K，每接触到新的节点就考虑给它加入到对应的 k-bucket 中。
于是还有一个问题是，是不是一定要每棵子树都要开一个 k-bucket。
因为机器初始化所有的 k-bucket 是需要时间的，可能很长一段时间内这些 k-bucket 里都没什么东西。
所以有另一种实现是初始只有一个 k-bucket，然后直接存，存满 K 个之后再根据子树的划分进行分裂。
我们的路由表就是由这些 k-bucket 组成的。&lt;&#x2F;p&gt;
&lt;p&gt;其二是目标节点的查询。
根据前文，源节点已经发现目标节点在某棵子树中，那么它只要往这个子树对应的 k-bucket 里的节点发查询请求就行了。
但是发一个肯定是不够的，于是我们定一个参数 α，让它在里面随机找 α 个节点发出去。
如果这个 k-bucket 里面节点的数量不足 α 个，那就要从相邻的 k-bucket 里面凑数，还不够的话，就继续再找相邻的凑。
如果节点刚刚接入网络，路由表还不完善的情况下，很有可能出现摁凑都凑不齐的情况，不过问题不大。
接下来收到查询请求的这 α 个节点，会各自在自己的路由表中找到目标节点对应的 k-bucket。
于是这些节点就从各自的 k-bucket 中又各自拿出 α 个节点，返回给源节点。
源节点这个时候就对这些收到的节点，每个都再发一遍查询请求。
这些节点继续各自返回 α 个节点。
直到找到目标节点。
在这个过程中，源节点还可以顺带更新自己的路由表，收到查询请求的节点也可以考虑把源节点更新到自己的路由表中。
当然如果目标节点不在线或者根本就不存在，那就爆炸了。
所以我们还需要定一下终止条件，但是我还没有查到比较靠谱的资料，所以我只能说一下我自己的猜想。
一种可能的策略是源节点肯定要哈希去重，避免反复骚扰同一个节点。
另一种可能的策略是，收到查询请求的节点如果目标节点对应的 k-bucket 中什么都没有，它就返回它自己，因为很可能这个节点跟目标节点的 ID 非常接近导致 k-bucket 里面东西本来就少，可以视为查询结果。
为什么我们可以把跟目标节点靠的近的节点作为查询结果呢？
因为 P2P 网络中节点随时可能会下线，因此我们精确查询某个节点是没有意义的。
如果某个资源仅仅存在某个节点上，它一旦下线那就拿不到了。
所以我们可以考虑把资源存在跟这个节点距离靠近的若干个节点上，这样还有抢救的机会。
另外，由于 Kademlia 算法存储资源的策略（见后文），被指定用来存储某个资源的节点 ID 可能根本就不存在，所以我们只能选取一些距离相近的节点来存储这个资源。&lt;&#x2F;p&gt;
&lt;p&gt;其三则是路由表的维护。
路由表一开始是空的啊，玩毛线。
所以我们需要用爱发电，买一些廉价服务器作为公共节点。
在一个新的节点接入网络时，直接把这些公共节点给他硬编码进到那些 k-bucket 里。
接下来就简单了，我们可以采用很多玄学策略来更新自己的路由表。
一种常见的策略就是对某个目标节点进行查询的时候就顺便维护路由表，因为查询时我们会得到很多中间途经的节点的信息。
对于每个途径的新节点，找到对应的 k-bucket，如果没有满就直接插入到末尾。
如果满了那就 ping 一下 k-bucket 的第一个节点，如果 ping 不通说明这个节点失效了，给他移除掉，新节点就能插入到末尾。
如果第一个节点 ping 通了那就把这个节点移动到末尾，然后直接把新节点扔掉不管就行了。
听起来有一点像内存的缓存替换策略。
这只是一种简单的实现，实际上应该也有更多的玄学替换策略。
另外如果 k-bucket 是会分裂的那种，那就只有在分裂到不能再分裂时才需要考虑替换。
当然，还有一种很容易理解的策略就是，每个节点可以周期性地更新路由表，比如说每隔半分钟就直接查询一个指定的 ID。
但是怎么确定查哪个 ID，这个就是跟资源相关的内容了。
不过还有一种我口胡的方法是：看哪个 k-bucket 比较空就往哪里查。
最后一个问题，一个节点如果要下线怎么办？
不用管，直接跑路就行了。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;zi-yuan-de-cun-chu-yu-fang-wen&quot;&gt;资源的存储与访问&lt;&#x2F;h2&gt;
&lt;p&gt;这一块就比较简单了。
DHT 是分布式哈希表，所以它维护的东西实际上是一堆 key-value 对。
假设每个资源都是文件或者一坨数据之类的东西，这个时候我们就可以把文件名、文件哈希或者这坨数据的关键字作为 key-value 对中的 value。
对这个 value 做一次哈希就能得到 key。
但是除了 key-value 对，DHT 还需要维护具体的数据，也就是文件的内容或者一坨数据。
比如说，如果用户想要查找某个文件，他需要把文件名做个哈希得到 key，然后在这个 DHT 中就能查找到这个文件名，以及文件中的内容。&lt;&#x2F;p&gt;
&lt;p&gt;然后问题来了，为什么要对文件名或者文件哈希啥的再做一次哈希呢，我直接拿文件名做 key 不行吗？
其实是为了方便处理。
因为这个 key 是有要求的：我们希望把 key 跟网络中节点的 ID 做一个对应关系。
也就是说我们希望对于一个 key，能够大概知道这个 key-value 对会被存储在什么地方。
Kademlia 算法要求 key 是一个无符号整型，它的位数跟节点 ID 的位数是一样的，并且它应当被存储在节点的 ID 跟 key 相近的节点上。
也就是说理想情况下，对于存储着一个 key-value 对的节点，它的节点 ID 是跟 key 相等的。&lt;&#x2F;p&gt;
&lt;p&gt;于是存储的策略就很简单了，如果用户在某个节点上发起了存储某个文件的操作，这个节点首先算出这个文件的 key，然后直接把 key 当作节点 ID 做一次查询操作。
接下来节点就会得到好几个距离这个 key 相近的节点的信息。
最后直接把文件发送给这些节点就行了。
另外，因为并不是所有节点时刻都在线，所以这一整套流程可能要每隔一段时间反复执行，使得这个文件能够在网络中得到充足的备份。
需要注意的是，这套流程里面包含了查询操作，根据前文我们知道查询是会更新路由表的，因此路由表实际上会随着周期性的存储而不断更新。&lt;&#x2F;p&gt;
&lt;p&gt;那么访问资源就更简单了，直接把 key 当作目标节点 ID 摁查。
查出来好几个比较近的节点，挨个索要这个资源就行了。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;xiao-xi-lei-xing&quot;&gt;消息类型&lt;&#x2F;h2&gt;
&lt;p&gt;前文中的实现细节其实是通过 RPC 消息来实现的，但是避免概念过多引起混乱就没有考虑放到前面。
所以最后再补充一下 Kademlia 规定的四种消息类型，现在来看就很好理解了。&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;PING —— 用于检查节点的在线状态。&lt;&#x2F;li&gt;
&lt;li&gt;STORE —— 要求一个在某个节点上存储一个 key-value 对。&lt;&#x2F;li&gt;
&lt;li&gt;FIND_NODE —— 给出一个目标节点的 ID，查询距离这个 ID 最近的 K 个节点。&lt;&#x2F;li&gt;
&lt;li&gt;FIND_VALUE —— 跟 FIND_NODE 相同，但是如果被查询的节点正好持有需要查询的 key-value 对，它将会把相应的 value 返回。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
</content>
	</entry>
	<entry xml:lang="zh">
		<title>NAT 穿透</title>
		<published>2022-04-25T23:13:27+00:00</published>
		<updated>2022-04-25T23:13:27+00:00</updated>
		<link href="https://paakmau.github.io/202204252313/" type="text/html"/>
		<id>https://paakmau.github.io/202204252313/</id>
		<content type="html">&lt;p&gt;P2P 网络需要考虑的一个非常折磨的细节就是并非每个节点都有固定的公网 IP。
于是就要开始考虑 NAT 穿透的问题了。&lt;&#x2F;p&gt;
&lt;span id=&quot;continue-reading&quot;&gt;&lt;&#x2F;span&gt;
&lt;p&gt;因为大多数用户都没有公网 IP 的需求，运营商通常默认是不会提供公网 IP 的。
于是也很少有人专门去折腾公网 IP，还能省点网费。
这个时候内网的主机想要跟公网的主机交互就需要使用 NAT 穿透来获取一个公网 IP。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;ji-ben-nat&quot;&gt;基本 NAT&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;jing-tai-nat&quot;&gt;静态 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;最简单的就是静态 NAT，就是给你主机的内网 IP 直接绑定到一个固定的公网 IP 上。
往外界发包的时候，源 IP 就会被交换机或者路由器替换为这个公网 IP。
外界的主机向你发送信息时，目标 IP 最初也是这个公网 IP，然后会被替换回内网 IP，最后才发回给你的主机。
在整个过程中你的主机只知道自己的内网 IP，对于这个公网 IP 是完全不知情的。
当然，运营商不可能真的给每个内网 IP 都配一个公网 IP。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;dong-tai-nat&quot;&gt;动态 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;所以接下来就是动态 NAT。
打个比方，整个小区可能只持有小区里电脑数量十分之一的公网 IP。
考虑到并不是每台电脑都时刻需要跟外界交互。
于是运营商只有在某台电脑向外界发包的时候才借一个公网 IP 给他，也就是在内网 IP 到公网 IP 的映射表中添加一条记录。
当交换机认定这个内网 IP 跟外界的通讯结束之后，这条记录就会被删除，也就是把借给它的公网 IP 强行要回来。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;napt&quot;&gt;NAPT&lt;&#x2F;h2&gt;
&lt;p&gt;基本 NAT 没有对端口进行映射。
然后我们发现实际上可用的端口空间是很大的，也就是说很多端口都是空闲的。
于是就考虑更细的粒度：把端口也做一个映射。
比方说来自内网中不同电脑的两个包，我可以直接给他映射到同一个公网 IP 上，随便给他分两个不同的端口就行了。
当然，同一台电脑通过不同端口发出的包也有可能被映射到不同的公网 IP 上。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;wan-quan-yuan-zhui-xing-nat&quot;&gt;完全圆锥型 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;一台内网机器向外网发出一个包，这个包的源 IP（内网）和源端口就会被固定映射到一个公网 IP 和端口，这个映射关系会维持足够长的时间。
外网的机器主动访问这个公网 IP 和端口也能够被转发到这个包的源 IP 和源端口，最终正确的被内网机器接收到。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;shou-xian-yuan-zhui-xing-nat&quot;&gt;受限圆锥型 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;在完全圆锥型的基础上，外网机器就算知道映射关系，往正确的 IP 和端口发包，这个包也会被交换机拦截。
只有在内网机器主动向这台外网机器发包之后，交换机才不会拦截这个包。&lt;&#x2F;p&gt;
&lt;p&gt;因为只有内网机器主动联系外网，映射关系才会建立起来。
并且大部分情况下，只有内网机器主动联系外网机器，这台外网机器才有可能知道正确的 IP 和端口。
所以交换机一旦发现有个外网的机器擅自向这个 IP 发包，它根本无法分辨到底是正常的请求还是 DDoS 攻击，为了安全性考虑，应当直接拒绝。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;duan-kou-shou-xian-yuan-zhui-xing-nat&quot;&gt;端口受限圆锥型 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;比普通的受限圆锥型多一个约束：外网主机的端口必须是固定的。&lt;&#x2F;p&gt;
&lt;p&gt;也就是说只有内网主机向外网主机的某个端口发包之后，从外网主机的这个端口发来的包才不会被拒绝。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;dui-cheng-nat&quot;&gt;对称 NAT&lt;&#x2F;h3&gt;
&lt;p&gt;非常离谱，只有内部 IP、内部端口、外部 IP、外部端口都相同的时候，这个包才会被映射到固定的公网 IP 和端口。
如果一台机器向不同外网 IP 或不同的端口发包（包初始的源 IP 和源端口都固定），会产生不同的映射，也就是说这些外网机器收到的包的源 IP 和源端口是不同的。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;nat-chuan-tou&quot;&gt;NAT 穿透&lt;&#x2F;h2&gt;
&lt;p&gt;接下来我们就要考虑不同内网中的两台机器之间建立连接的问题了，也就是 NAT 穿透的问题。
首先两台机器不知道自己映射的公网 IP 和端口，也不知道对方的，发包都不知道往哪里发。&lt;&#x2F;p&gt;
&lt;p&gt;因此只有这两台肯定是不行的，这个时候我们需要一台有公网 IP 的公共服务器。
假设这两台内网的机器是 A 和 B，公共服务器是 C。
首先 A 和 B 各自跟 C 建立连接。
于是 C 就拿到了 A 和 B 的公网 IP 和端口，它只要把这些信息告诉 A 和 B，理想情况下 A 和 B 就能互相连接了。
没错，这是理想情况，比如说基本 NAT 或者完全圆锥型 NAT。
而操蛋情况，比方说受限圆锥型，如果 A 没有主动发包，B 即使对着 A 的正确地址发包，也会被交换机拒绝。
这个时候就需要打洞了。&lt;&#x2F;p&gt;
&lt;p&gt;假设机器 A 的 NAT 是受限圆锥型，C 就需要通知 A 向 B 的方向打洞，也就是说 A 需要发一个以 B 的公网 IP 为目标 IP 的包。
这样 A 那边的交换机就会允许 B 的这个公网 IP 向 A 发包。
于是 B 直接对着 A 的公网 IP 和端口发包，A 就能收到了。&lt;&#x2F;p&gt;
&lt;p&gt;如果 A 的 NAT 是端口受限圆锥型，C 就需要向 A 传达 B 跟 C 建立连接时用的端口，然后 A 要向 B 的公网 IP 的这个端口发包。
最后 B 需要使用跟 C 建立连接的端口向 A 的公网 IP 和端口发包，才能跟 A 建立连接。&lt;&#x2F;p&gt;
&lt;p&gt;如果 A 那边的 NAT 是对称 NAT，那么就不用考虑让 A 打洞了。
因为 B 如果要跟 A 建立连接，交换机会给 A 映射到另一个地址，C 和 B 都没有办法知道。
这个时候只能考虑让 B 打洞，然后要求 A 主动去连 B。
如果很倒霉 B 那边也是对称 NAT，那就没救了。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;udp-tong-xin&quot;&gt;UDP 通信&lt;&#x2F;h2&gt;
&lt;p&gt;上述打洞的方法实际上是针对 UDP 来说的。
因为 UDP 允许在同一个端口上开多个套接字。
所以，A 和 B 用来跟 C 通信的端口同时还能用来进行 A 和 B 之间的通信。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;tcp-tong-xin&quot;&gt;TCP 通信&lt;&#x2F;h2&gt;
&lt;p&gt;如果想要 A 和 B 进行 TCP 通信就麻烦了，因为 TCP 不允许多个套接字绑定到同一个端口上。
实际上这并不是协议的问题，而是接口的问题。
不过好在很多操作系统实际上是支持一种特殊的 TCP 套接字选项，一般叫做 &lt;code&gt;SO_REUSEADDR&lt;&#x2F;code&gt;。
这个选项允许多个套接字绑定到同一个局部地址上，前提是这些套接字必须全部都启用这个选项。
把相关的套接字都开启 &lt;code&gt;SO_REUSEADDR&lt;&#x2F;code&gt; 之后，A 和 B 就能以类似于 UDP 的方式建立 TCP 通信。&lt;&#x2F;p&gt;
</content>
	</entry>
	<entry xml:lang="zh">
		<title>P2P 网络学习</title>
		<published>2022-04-25T20:03:05+00:00</published>
		<updated>2022-04-25T20:03:05+00:00</updated>
		<link href="https://paakmau.github.io/202204252003/" type="text/html"/>
		<id>https://paakmau.github.io/202204252003/</id>
		<content type="html">&lt;p&gt;P2P 网络是一个有别于中心化网络的东西，本文简单介绍一些实现 P2P 网络的思路。&lt;&#x2F;p&gt;
&lt;span id=&quot;continue-reading&quot;&gt;&lt;&#x2F;span&gt;&lt;h2 id=&quot;zhong-xin-hua-wang-luo&quot;&gt;中心化网络&lt;&#x2F;h2&gt;
&lt;p&gt;首先介绍一下中心化的网络，对于中心化的网络而言，资源的存取非常简单。
大概就是把资源存放在一台服务器或者一组集群上，客户端直接访问这些服务器就可以获取资源。
中心化的设计要简单很多，而且不需要花很多功夫就能得到很好的性能。
主要问题是服务器的维护本身需要成本，而且资源存取的速度会受到服务器带宽和集群的限制，更高的带宽与更庞大的集群意味着更高的成本。
如果我们想要做一些用爱发电的事情，做成中心化是很难维持的。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;hun-he-mo-xing&quot;&gt;混合模型&lt;&#x2F;h2&gt;
&lt;p&gt;于是我们发现如果能直接把资源放在客户端上，就能缓解服务端的压力，很多年前著名的 Napster 就是采用的这个做法。
具体来说，当一个客户端请求某个资源时，服务端负责返回这个资源存在哪个客户端上，于是客户端之间就能直接连接共享这个资源，服务端也就不需要存储资源了。
实际上就是服务端仅仅起到一个对客户端之间进行路由的作用，而资源的存储就完全交给客户端。&lt;&#x2F;p&gt;
&lt;p&gt;这就是最早的 P2P 模型：混合模型。
这种想法其实已经很节约了，然而服务端的维护成本对于个人来说仍然难以承受，用爱发电仍然很难。
而且它仍然具有中心化的特点：如果服务端炸了，那还是一样全部爆炸。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;fei-jie-gou-hua-wang-luo&quot;&gt;非结构化网络&lt;&#x2F;h2&gt;
&lt;p&gt;再接下来我们考虑能不能把路由的功能也做成分布式的。
有一种简单的做法，P2P 网络中每个节点需要查询某个资源时，它向它的已知的一些节点（称为邻居）发送查询请求，邻居再把它的请求发送给邻居的邻居，直到找到资源。
这种做法一听就非常操蛋，我想要找一个资源就要把消息一层一层转发给网络中的一大堆节点，我的节点本身也要时常处理网络中其他人发来的查询请求。
所以直接摁发肯定是不行的，优化的策略有很多：避免广播、约束转发的上限、优化网络的拓扑结构等等。&lt;&#x2F;p&gt;
&lt;p&gt;那么这个网络拓扑是怎么构建起来的呢？
这种思路要求每个节点应当维护一个邻居的列表（路由表），初始时当然是空的，所以需要有一些用爱发电的公共节点作为初始的邻居。
每个节点会跟邻居进行通信，动态地获取网络上可用的其他节点，据此来维护自己的路由表，并做一些持久化。
通过邻居关系连接各个节点，网络拓扑就建立起来了。&lt;&#x2F;p&gt;
&lt;p&gt;这其实也就是出现在混合模型之后的非结构化 P2P，典型代表有 Gnutella 协议和比特币。
它的优点很明显，节点之间路由关系的维护可以直接交给各个节点，只需要购买一些廉价服务器做公共节点，整个 P2P 网络自己就能慢慢构建起来。
缺点也很明显：寻找资源需要消耗大量的网络资源；查询速度非常慢；即使目标节点在线也有可能查不到资源。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;jie-gou-hua-wang-luo&quot;&gt;结构化网络&lt;&#x2F;h2&gt;
&lt;p&gt;继非结构化网络之后，结构化的 P2P 出现了，实际上它是在拓扑结构上进行了特殊优化。
它对 P2P 网络的拓扑结构有特定的要求，并且能够确保节点对资源的访问是高效的且稳定的。
典型的例子就是以太坊。&lt;&#x2F;p&gt;
&lt;p&gt;最常见的结构化网络就是分布式哈希表（DHT），它使用一个哈希函数来把每个文件分配给对应的节点管理。
这样整个网络就构成一个哈希表，节点想要查询某个资源，就用它资源名的哈希值在表中查询到对应的节点获取该资源。
当然资源的查询请求仍然是需要在节点之间互相转发的，因此就需要对拓扑结构进行优化，以保证转发的路径不要太离谱。
具体来说就是路由表的维护策略，比如 Kademlia 算法采用二分的思路进行设计。
因为 DHT 是哈希表，所以理所当然模糊匹配是比较难实现的。
另外 P2P 网络中在线节点不稳定，拓扑结构变化会非常频繁，优秀的 DHT 设计应当充分考虑这一问题。
负载均衡也是 DHT 需要考虑的问题。&lt;&#x2F;p&gt;
</content>
	</entry>
</feed>
